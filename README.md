## Evaluation

Ablation study results (retrieval chunk-type filter): see [`eval/reports/ablation.md`](eval/reports/ablation.md).
Conclusion: `all_chunks` outperforms `speech_only` (65.0% vs 16.7% faithfulness on the current dataset) — screen-share chunks add retrieval value, don't filter them out.
