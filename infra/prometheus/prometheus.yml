global:
  scrape_interval: 15s

scrape_configs:
  - job_name: video-api
    metrics_path: /metrics
    static_configs:
      # host.docker.internal reaches the host machine from Docker Desktop on Windows/Mac
      - targets: ["host.docker.internal:8000"]
